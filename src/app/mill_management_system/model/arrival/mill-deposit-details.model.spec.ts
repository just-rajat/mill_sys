import { MillDepositDetails } from './mill-deposit-details.model';

describe('MillDepositDetails', () => {
  it('should create an instance', () => {
    expect(new MillDepositDetails()).toBeTruthy();
  });
});
