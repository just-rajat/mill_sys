import { StateDistrictTaluka } from './state-district-taluka.model';

describe('StateDistrictTaluka', () => {
  it('should create an instance', () => {
    expect(new StateDistrictTaluka()).toBeTruthy();
  });
});
